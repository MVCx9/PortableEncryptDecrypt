# PortableEncryptDecrypt
Encrypt and decrypt messages application with RSA algorithm using public key storage in .pem file (certificate) and private key storage in .p12 file (protected with password)
